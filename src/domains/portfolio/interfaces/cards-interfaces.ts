export interface WorkCardInfo {
    title: string;
    subtitle: string;
    desc: string;
    img: string;
    icon: string;
    codeLink?: string;
    viewLink?: string;
}

export interface SocialMediaInfo {
    name: string,
    icon: string,
    url: string
}